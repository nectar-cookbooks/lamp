<?php if (!defined('PmWiki')) exit();
## Generated by Chef from adodb_conf.php.erb ##
<% node['lamp']['adodb']['databases'].each() do |name, attrs|
   password = attrs['password'] || node['lamp']['adodb']['default_password']
   raise "Password required for ADODB database #{name}" unless password
%>

$Databases['<%= name %>'] = array(
  'driver' => '<%= attrs["driver"] || "mysql" %>',
  'hostname' => '<%= attrs["hostname"] || "localhost" %>',
  'database' => '<%= attrs["database"] || name  %>',
  'username' => '<%= attrs["username"] || "adodbUser" %>',
  'password' => '<%= password %>');
<% end %>

include_once "$FarmD/cookbook/adodb-connect.php";
