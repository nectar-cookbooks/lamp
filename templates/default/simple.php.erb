<?php if (!defined('PmWiki')) exit();
# Initial configuration generated by Chef.  
$WikiTitle = "<%= node['lamp']['pmwiki']['wiki_title'] %>";
<% if node['lamp']['pmwiki']['page_logo_url'] %>
$PageLogoUrl = "<%= node['lamp']['pmwiki']['page_logo_url'] %>";
<% end %>
<% if node['lamp']['pmwiki']['script_url'] %>
$ScriptUrl = "<%= node['lamp']['pmwiki']['script_url'] %>";
<% end %>
<% if node['lamp']['pmwiki']['pub_dir_url'] %>
$PubDirUrl = "<%= node['lamp']['pmwiki']['pub_dir_url'] %>";
<% end %>

$DefaultPasswords['admin'] = 
  <%= (node['lamp']['pmwiki']['admin_password'] ? "'#{node['lamp']['pmwiki']['admin_password']}'" : "crypt('yOrEaLlyReAlLyNeEdToChAnGeThIs')") %>;

$EnableUpload = <%= node['lamp']['pmwiki']['enable_upload'] ? 1 : 0 %>;
$DefaultPasswords['upload'] = 
  <%= (node['lamp']['pmwiki']['upload_password'] ?	
       "'#{node['lamp']['pmwiki']['upload_password']}'" :
       "crypt('yOrEaLlyReAlLyNeEdToChAnGeThIsAsWeLl')") %>;

<% if node['lamp']['pmwiki']['tz'] then %>
date_default_timezone_set("<%= node['lamp']['pmwiki']['tz'] %>");
<% end %>

$TimeFmt = '%B %d, %Y, at %I:%M %p EST';

<% if node['lamp']['pmwiki']['auto_config'] then %>
foreach ( scandir($LocalDir) as $filename )
    if ( $filename != 'config.php' && preg_match('/.php$/', $filename) )
        include_once("$LocalDir/$filename");
<% end %>