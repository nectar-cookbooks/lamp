<?php if (!defined('PmWiki')) exit();
## Generated by Chef from converthtml_conf.php.erb ##

# PmWiki markup does not support all of the HTML markup so a 100% conversion 
# is not possible. However, PmWiki can make replacements to the text as it 
# is being edited or saved. ConvertHTML implements a relatively comprehensive 
# set of rules for converting HTML tags to wiki markup.

if ($action == 'edit') include_once("$FarmD/cookbook/convert-html.php");
